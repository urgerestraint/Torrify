# Prevent package-lock version conflicts
package-lock=true

# Use faster registry if available
registry=https://registry.npmjs.org/

# Increase timeout for large packages
fetch-timeout=60000

# Reduce log output
loglevel=error
